// import { useEffect, useState } from 'react'

// https://developer.mozilla.org/en-US/docs/Web/API/Window/matchMedia

//  window.matchMedia('(max-width: 768px)').matches -> true or false
//  window.matchMedia(query).addEventListener('change', () => {}) -> se ejecuta cuando cambia el valor de la media query

function useMediaQuery() {
    return true
}

export default useMediaQuery